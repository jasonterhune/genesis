Activate each of these environments and install the packages saved
in each requirements files:

$ workon tb_dev
$ pip install -r requirements/development.txt

$ workon tb_test
$ pip install -r requirements/testing.txt
